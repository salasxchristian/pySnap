<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Disable App Sandbox to allow network connections and keychain access -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
    
    <!-- Allow network client connections for vCenter communication -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Allow server connections (may be needed for some VMware APIs) -->
    <key>com.apple.security.network.server</key>
    <true/>
    
    <!-- Allow file access for logs and configuration -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    
    <!-- Hardened runtime exceptions -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Allow DYLD environment variables (needed for some Python libraries) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>